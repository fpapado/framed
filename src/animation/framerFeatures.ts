/** File that creates a separate chunk for the framer-motion features that we use. Used to load them lazily.
 * @see https://www.framer.com/docs/guide-reduce-bundle-size/#lazy-loading
 */
import { domMax } from "framer-motion";
export default domMax;
